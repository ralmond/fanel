test_that("mstep {mstep}", {
  ## mstep(obj, data, ..., its = 3, control = list(), workers = Workers$new())
})
test_that("mstep {lprob}", {
  ## lprob(obj, data, par=pvec(obj))
})
test_that("mstep {pvec}", {
  ## pvec(obj)
})
test_that("mstep {pvec<-}", {
  ## pvec(obj) <- value
})

